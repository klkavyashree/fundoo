import { LabelsearchPipe } from './labelsearch.pipe';

describe('LabelsearchPipe', () => {
  it('create an instance', () => {
    const pipe = new LabelsearchPipe();
    expect(pipe).toBeTruthy();
  });
});
